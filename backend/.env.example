# Database Connection (PostgreSQL)
DATABASE_URL="postgresql://postgres:123456@localhost:5432/stylesense_dev?schema=public"

# Server Configuration
PORT=4000
NODE_ENV=development
APP_BASE_URL="http://localhost:4000"
FRONTEND_URL="http://localhost:5173"

# JWT Authentication
JWT_SECRET="your-jwt-secret-key-change-in-production"
JWT_EXPIRES_IN="7d"

# Email Provider Configuration (sandbox | resend | smtp)
EMAIL_PROVIDER="sandbox"
RESEND_API_KEY=""
SMTP_HOST=""
SMTP_PORT=587
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="Sanskar from StyleSense AI <outreach@stylesense.ai>"

# AI Agent Configuration (optional live API key; grounded mock active if empty)
OPENAI_API_KEY=""
GEMINI_API_KEY=""
